#ifndef _LARGEFUNCTIONS_H
#define _LARGEFUNCTIONS_H

#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

#include "helperFunctions.h"

#define PI 3.1415926535897932384626433832795

/** 
 * Function explanation... What it does.
 * @param: What Parameters does the function take?
 * @return: What value, if any, does the function return.
 **/
void time();

/** 
 * Function explanation... What it does.
 * @param: What Parameters does the function take?
 * @return: What value, if any, does the function return.
 **/
void temp();

/** 
 * Function explanation... What it does.
 * @param: What Parameters does the function take?
 * @return: What value, if any, does the function return.
 **/
unsigned long long int fact(int n);

/** 
 * Function explanation... What it does.
 * @param: What Parameters does the function take?
 * @return: What value, if any, does the function return.
 **/
void matrix_sum();

/** 
 * Function explanation... What it does.
 * @param: What Parameters does the function take?
 * @return: What value, if any, does the function return.
 **/
void matrix_product();

/** 
 * Function explanation... What it does.
 * @param: What Parameters does the function take?
 * @return: What value, if any, does the function return.
 **/
void matrix_transpose();

/** 
 * Function explanation... What it does.
 * @param: What Parameters does the function take?
 * @return: What value, if any, does the function return.
 **/
double powerfn(double x, int n);

#endif